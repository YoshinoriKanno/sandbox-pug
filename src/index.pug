doctype html
html(lang="ja")
  head
    title #{title}
    meta(name="description" content=description)
  body
    h1#heading 見出し
    if isDemo === true
      p.paragraph.-demo.
        デモの本文です。isDemoがtrueであれば表示されます。
    else
      p.paragraph.
        本文です。isDemoがfalseであれば表示されます。
